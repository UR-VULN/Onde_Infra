# name: Deploy Backend

# on:
#   repository_dispatch:
#     types:
#       - deploy-backend

# env:
#   AWS_REGION: ap-northeast-2

# jobs:
#   deploy:
#     name: Deploy to Backend EC2s
#     runs-on: ubuntu-latest

#     strategy:
#       # 두 인스턴스 동시 배포 (HA 유지를 위해 sequential로 변경 가능)
#       matrix:
#         include:
#           - name: backend-1 (api-module)
#             instance_secret: BACKEND_EC2_1_INSTANCE_ID
#             ecr_repo: onde/backend-user
#             container_name: onde-backend-user
#             port: "8080:8080"
#           - name: backend-2 (admin-module)
#             instance_secret: BACKEND_EC2_2_INSTANCE_ID
#             ecr_repo: onde/backend-seller
#             container_name: onde-backend-seller
#             port: "8080:8080"

#     steps:
#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Deploy ${{ matrix.name }} via SSM
#         env:
#           IMAGE_TAG: ${{ github.event.client_payload.image_tag }}
#           AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
#           ECR_REPO: ${{ matrix.ecr_repo }}
#           CONTAINER_NAME: ${{ matrix.container_name }}
#           PORT: ${{ matrix.port }}
#         run: |
#           INSTANCE_ID=$(echo "${{ secrets[matrix.instance_secret] }}")
#           ECR_REGISTRY="${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com"
#           IMAGE="${ECR_REGISTRY}/${ECR_REPO}:${IMAGE_TAG}"

#           COMMAND_ID=$(aws ssm send-command \
#             --instance-ids "$INSTANCE_ID" \
#             --document-name "AWS-RunShellScript" \
#             --parameters commands="[
#               \"ECR_REGISTRY=${ECR_REGISTRY}\",
#               \"IMAGE=${IMAGE}\",
#               \"# Docker 미설치 시 설치\",
#               \"if ! command -v docker &>/dev/null; then apt-get update -y && apt-get install -y docker.io && systemctl enable --now docker; fi\",
#               \"aws ecr get-login-password --region ${AWS_REGION} | docker login --username AWS --password-stdin \$ECR_REGISTRY\",
#               \"docker pull \$IMAGE\",
#               \"docker stop ${CONTAINER_NAME} || true\",
#               \"docker rm ${CONTAINER_NAME} || true\",
#               \"docker run -d --name ${CONTAINER_NAME} --restart always -p ${PORT} \$IMAGE\",
#               \"docker image prune -f\",
#               \"echo 'Deploy done: '\$IMAGE\"
#             ]" \
#             --timeout-seconds 300 \
#             --query "Command.CommandId" \
#             --output text)

#           echo "SSM Command ID: $COMMAND_ID"

#           aws ssm wait command-executed \
#             --command-id "$COMMAND_ID" \
#             --instance-id "$INSTANCE_ID"

#           STATUS=$(aws ssm get-command-invocation \
#             --command-id "$COMMAND_ID" \
#             --instance-id "$INSTANCE_ID" \
#             --query "Status" \
#             --output text)

#           echo "${{ matrix.name }} 배포 상태: $STATUS"

#           if [ "$STATUS" != "Success" ]; then
#             aws ssm get-command-invocation \
#               --command-id "$COMMAND_ID" \
#               --instance-id "$INSTANCE_ID" \
#               --query "StandardErrorContent" \
#               --output text
#             exit 1
#           fi

#           echo "✅ ${{ matrix.name }} 배포 완료 (tag: $IMAGE_TAG)"
