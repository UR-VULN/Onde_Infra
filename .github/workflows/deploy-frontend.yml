# name: Deploy Frontend

# on:
#   repository_dispatch:
#     types:
#       - deploy-frontend

# env:
#   AWS_REGION: ap-northeast-2

# jobs:
#   deploy:
#     name: Deploy to Frontend EC2
#     runs-on: ubuntu-latest

#     steps:
#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Deploy via SSM — Frontend EC2
#         env:
#           INSTANCE_ID: ${{ secrets.FRONTEND_EC2_INSTANCE_ID }}
#           IMAGE_TAG: ${{ github.event.client_payload.image_tag }}
#           AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}
#         run: |
#           COMMAND_ID=$(aws ssm send-command \
#             --instance-ids "$INSTANCE_ID" \
#             --document-name "AWS-RunShellScript" \
#             --parameters 'commands=[
#               "ECR_REGISTRY='$AWS_ACCOUNT_ID'.dkr.ecr.'${{ env.AWS_REGION }}'.amazonaws.com",
#               "IMAGE=$ECR_REGISTRY/onde/frontend:'$IMAGE_TAG'",
#               "aws ecr get-login-password --region '${{ env.AWS_REGION }}' | docker login --username AWS --password-stdin $ECR_REGISTRY",
#               "docker pull $IMAGE",
#               "docker stop onde-frontend || true",
#               "docker rm onde-frontend || true",
#               "docker run -d --name onde-frontend --restart always -p 80:80 $IMAGE",
#               "docker image prune -f"
#             ]' \
#             --timeout-seconds 300 \
#             --query "Command.CommandId" \
#             --output text)

#           echo "SSM Command ID: $COMMAND_ID"

#           # 완료될 때까지 대기 (최대 5분)
#           aws ssm wait command-executed \
#             --command-id "$COMMAND_ID" \
#             --instance-id "$INSTANCE_ID"

#           STATUS=$(aws ssm get-command-invocation \
#             --command-id "$COMMAND_ID" \
#             --instance-id "$INSTANCE_ID" \
#             --query "Status" \
#             --output text)

#           echo "Deploy status: $STATUS"

#           if [ "$STATUS" != "Success" ]; then
#             aws ssm get-command-invocation \
#               --command-id "$COMMAND_ID" \
#               --instance-id "$INSTANCE_ID" \
#               --query "StandardErrorContent" \
#               --output text
#             exit 1
#           fi

#           echo "✅ Frontend 배포 완료 (tag: $IMAGE_TAG)"
